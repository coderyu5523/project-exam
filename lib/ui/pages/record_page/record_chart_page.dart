import 'package:flutter/material.dart';

class RecordChartPage extends StatelessWidget {
  const RecordChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("통계 페이지 내용"),
    );;
  }
}
